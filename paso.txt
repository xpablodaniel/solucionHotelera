⚓ Las instrucciones a modificar


Ajuste del Voucher Diario de Balneario individual

Trabajar únicamente sobre el formulario/manual del Voucher Diario de Balneario.

No implementar nuevas funcionalidades ni modificar el flujo histórico de Balneario generado desde reservas.

Mantener el título:
Voucher Diario de Balneario
El texto descriptivo no debe mencionar "Alicante" ni ningún nombre específico de balneario.
El formulario debe solicitar únicamente:
Nombre y apellido
DNI
Hotel
Habitación
Fecha
Cantidad de personas
Eliminar del formulario fechaIngreso y fechaEgreso.
Crear/usar un único campo fecha, precargado con la fecha actual y modificable por el operador.
La fecha debe utilizar el mismo criterio de formato que el Voucher Diario de Comidas.
El nombre del PDF debe construirse con DNI y fecha, por ejemplo:
voucher_balneario_12345678_2026-09-26.pdf
Mantener la plantilla histórica y las coordenadas ya calibradas.
Mantener los casilleros DIA 1 a DIA 5 sin marcarlos automáticamente.
No agregar todavía enlaces entre Voucher de Comidas y Voucher de Balneario.
No modificar client/mealVoucher.html.
No modificar python/mealVoucher/source/voucherDiario.jpg.
No modificar paso.txt.
No modificar processReservations.js, Rooming, CSV, MAP/PC ni el flujo histórico de Balneario.
Ejecutar las pruebas existentes y agregar/modificar únicamente las pruebas necesarias para reflejar el nuevo contrato.

Antes de modificar positions.json, verificar si las posiciones actuales permiten representar correctamente la nueva fecha única. Si requieren cambio, informar primero qué posición se utilizará; no modificar coordenadas históricas innecesariamente.

Y agregaría una última cláusula:

No crear todavía ninguna card nueva en index.html ni decidir la navegación entre ambos vouchers. Esa decisión queda pendiente para una etapa posterior.

🧭 Y una cuestión estratégica

No destruyó el mecanismo histórico de tres vouchers por A4 para resolver el individual.

Está agregando una capa:

VOUCHER ALICANTE HISTÓRICO
        │
        ├── flujo desde reservas
        │       └── 3 vouchers por hoja
        │
        └── emisión manual
                └── 1 voucher individual

Esa separación es exactamente lo que queremos.

Así que no hay que volver a empezar. Hay que corregir el contrato del formulario y dejar el motor PDF tranquilo.

Y después de esa corrección, hacemos lo que nos gusta a los dos:

prueba visual → tests → git diff → conteo de minicloridianos → commit. ⚓