Ese es exactamente el siguiente paso. 🎶🧉

Y con buildRoomingReport() ya probado con 12/12, tenemos una separación muy saludable:

CSV
 ↓
parseCSV()
 ↓
processReservations()
 ↓
buildRoomingReport()
 ↓
reporte.filas
 ↓
exportRoomingCsv()
 ↓
reservas_ingresan.csv
El nuevo módulo

Crearía:

src/output/roomingCsv.js
tests/output/testRoomingCsv.js

con una función pura del estilo:

exportRoomingCsv(filas)

Su única responsabilidad sería:

Recibir reporte.filas.
Generar la cabecera del CSV.
Convertir cada fila a CSV.
Escapar correctamente comas, comillas y saltos de línea.
Devolver el texto CSV.

No debería:

volver a agrupar pasajeros;
ordenar habitaciones;
calcular capacidades;
consultar inventario;
modificar reservas;
decidir quién comparte habitación.

Todo eso ya ocurrió antes.

Contrato

La entrada:

const reporte = buildRoomingReport(reservas);

const csv = exportRoomingCsv(reporte.filas);

La salida sería un string:

Nro. habitación,Fecha de ingreso,Fecha de egreso,...
238,20/09/2026,27/09/2026,3,...
238,20/09/2026,27/09/2026,3,...
238,20/09/2026,27/09/2026,3,...

Y recién después, en una capa todavía más externa, podríamos hacer:

fs.writeFileSync(
    "reservas_ingresan.csv",
    csv,
    "utf8"
);

Así el módulo de salida no necesita ni siquiera saber dónde se guarda el archivo.

Las pruebas que haría

Como mínimo:

🟢 genera cabecera
🟢 genera una fila
🟢 conserva el orden recibido
🟢 conserva voucher
🟢 conserva Cantidad plazas
🟢 escapa comas
🟢 escapa comillas
🟢 escapa campos vacíos
🟢 array vacío
🟢 valida entrada

Y una prueba particularmente importante:

DUMMY-001
DUMMY-002

en la misma habitación 238 deben permanecer como vouchers distintos.

Eso nos garantiza que el exportador es realmente un serializador y no vuelve a interpretar el negocio.

Después

Una vez que testRoomingCsv.js quede verde:

buildRoomingReport()
        ↓
exportRoomingCsv()
        ↓
reservas_ingresan.csv

y podremos comparar la estructura producida con la salida original que recuperamos en la auditoría.

Yo haría ese módulo ahora, antes de tocar Rooming PC o vouchers. Es una etapa pequeña, comprobable y nos deja prácticamente reconstruido el primer producto de salida original. 🌸

## Estado de esta etapa

El exportador CSV ya fue construido:

src/output/roomingCsv.js
tests/output/testRoomingCsv.js

`exportRoomingCsv(filas)` recibe únicamente las filas del reporte, genera la cabecera compatible y escapa comas, comillas y saltos de línea sin volver a interpretar el negocio.

La prueba enfocada y la suite completa quedaron verdes: 13 tests, 0 fallos.

El siguiente paso es comparar el texto generado con el contrato del listado original y, recién después, evaluar una capa externa que escriba `reservas_ingresan.csv`.